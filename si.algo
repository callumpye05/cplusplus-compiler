
algorithme //array
variables
	T : tableau de entier
	i : entier
	x : entier
début
	T <- Tableau(10);
	pour i allant de 1 à longueur(T) faire
		T[i] <- 2;
	fin pour
	pour tout x dans T faire 
		écrire("la valeur est :");
		écrire(x);
	fin pour
fin
